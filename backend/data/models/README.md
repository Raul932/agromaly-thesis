# This directory stores trained LSTM Autoencoder model weights.
# Files generated by the training notebook:
#   - lstm_autoencoder.pt    (PyTorch state_dict)
#   - scaler_params.json     (MinMaxScaler parameters)
#   - model_config.json      (architecture hyperparameters + threshold)
